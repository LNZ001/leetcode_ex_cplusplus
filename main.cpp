#include <iostream>
#include <vector>
#include <list>
using namespace std;

int main() {
    vector<int> a;
    vector<int> b;
    list<int> c;
    a.push_back(7544);
    a.push_back(23);
    std::cout << a.size() << std::endl;
    return 0;
}
